% ---------------------------
%
% Bachelor Thesis Style File
% Fabian Brix 2013
%
%---------------------------

% redefining stuff
% using packages etc

\usepackage[top=3cm, bottom=3cm, left=3cm, right=3cm]{geometry}

% change line spacing to 1.5
\linespread{1.3}
%\usepackage{fontspec}
%\setmainfont{Arial}

% anything preceeded by a blank line or defined by \par.. is classified as a new paragraph and the first line is indented by default
\setlength{\parindent}{0in}
\usepackage{setspace}

\usepackage{float}
% needed to place figures exactly at there specified locations [H]

% wrap text around figures, let figure protrude into the margin
\usepackage{wrapfig}
% located in synaptic package: texlive-latex-extra
\usepackage{sidecap}

\usepackage{abstract} 
% may be of use to float abstract to the right, or to narrow the text 

\usepackage{lineno}
% line numbers

\usepackage{color}

\usepackage{graphicx}
% for including images - eps format preferred
\usepackage{tikz}
% for drawing stuff

\usepackage{subfig}

\usepackage{amsmath, amssymb}
% packages published by the american mathematical society
% for more info visit http://tex.stackexchange.com/questions/32100/what-does-each-ams-package-do

\usepackage{listings}
% include source code of any programming language

\definecolor{mybeige}{rgb}{1,1,.7}
\definecolor{lightgray}{rgb}{.98,.98,.98}
\definecolor{myred}{rgb}{.86,.22,.22}

%% parameters to set
\newcommand*{\supervisor}[1]{\def\supname{#1}}
\newcommand*{\examiner}[1]{\def\examname{#1}}
\newcommand*{\authors}[1]{\def\authornames{#1}}
\newcommand*{\email}[1]{\def\emailaddr{#1}}
\newcommand*{\universitylogo}[1]{\def\unilogo{#1}}
\newcommand*{\universitysignet}[1]{\def\unisignet{#1}}
\newcommand*{\department}[1]{\def\deptname{#1}}
\newcommand*{\faculty}[1]{\def\facname{#1}}
\newcommand*{\group}[1]{\def\groupname{#1}}
\newcommand*{\thesistype}[1]{\def\typename{#1}}
\newcommand*{\matrnr}[1]{\def\matrnumber{#1}}

\renewcommand\maketitle{
    \hypersetup{pdftitle={\@title}}
    \hypersetup{pdfauthor=\authornames}
    \thispagestyle{empty}
    \begin{titlepage}
        \begin{center}
            \vspace*{\fill}
            {\HUGE \textsf\@title \par}
            \vfill
            {by\\ \large \textit{\authornames} \par}
            \bigskip
            \bigskip
            { {\Large\textsf\typename} \par}
            \bigskip
            {submitted in partial fulfillment\\ of the requirements for the degree of \par}
            \bigskip
            {\Large \textsf{BSc in Computer Science} \par}
            \bigskip
            {at the\quad\includegraphics{\unilogo} \par}
            \bigskip
            \bigskip
            {\large \par}
            \bigskip
            \textbf{Examiner}\\ \examname \\
            \bigskip
            \textbf{Supervisors}\\ \supname \\
            \bigskip
            {\groupname \par}
            {\deptname \par}
            {\facname \par}
            \vfill
            {\@date \par}
            \bigskip

            \bigskip
            {\includegraphics[width=1.5cm]{\unisignet} }
        \end{center}
        \par
        \vspace*{\fill}
    \end{titlepage}
}

\newcommand{\numold}[1]{ {\addfontfeature{Numbers=OldStyle}#1}}
%\lstnewenvironment{scode}
%{
\lstset{ 
    language=c++,
    backgroundcolor=\color{lightgray},
    commentstyle=\tiny\bfseries\color{myred},
    basicstyle=\tiny\normalfont,
    numberstyle=\numold,
    numbers=\left,
    xleftmargin=15pt,
    framexleftmargin=15pt,
    xrightmargin=5pt,
    framexrightmargin=10pt,
    xtopmargin=10pt,
    breaklines=true, % Zeilen umbrechen
    showstringspaces=false
}%}
%{}
\lstloadlanguages{
    C++
    Java
}

\definecolor{mygreen}{rgb}{0.24,0.8,.16}

\usepackage{caption}

\DeclareCaptionFont{white}{\color{white}}
\DeclareCaptionFormat{listing}{\colorbox{mygreen}{\parbox{\textwidth}{#1#2#3}}}
\captionsetup[lstlisting]{format=listing, labelfont=white,textfont=white}

\DeclareMathSizes{11}{11}{11}{11}
% < surrounding text size>, <math text size>, <math script size>, <math scriptscript size>

\newcommand{\viscomment}[1]{\textcolor{red}{\textbf{\textit{#1}}}}
% command for adding comments to the thesis that are visible to the reader

\newcommand{\vect}[1]{\textbf{#1}} %{\vec{#1}}
% custom vector definition, makes it possible to change all vectors to bold notation

\newcommand{\twopartdef}[4]
{
    \left\{
        \begin{array}{ll}
            #1 & \mbox{if } #2 \\
            #3 & \mbox{if } #4 
        \end{array}
        \right.
    }

    \renewcommand*{\contentsname}{Table of Contents}
    \usepackage[pdftex]{hyperref}
    \chapterstyle{ell}



%% Count footnotes through whole document
    \counterwithout{footnote}{chapter}

%% Load up fancy headers and footers package
    \RequirePackage{fancyhdr}
    \pagestyle{fancy}

%% Now reset the headers and footers
    \fancyhead{}
    \fancyfoot{}
    \cfoot{}

    \renewcommand{\chaptermark}[1]{\markboth{#1}{}}
    \renewcommand{\sectionmark}[1]{\markright{#1}{}}

%% bring the style into effect
%% make the odd pages have the section name on the top right
    \fancyhead[LO]{\nouppercase\leftmark}
    \fancyhead[RO]{\thepage}
%% make the even pages have the chapter name on the top left
    \fancyhead[RE]{\nouppercase\leftmark}
    \fancyhead[LE]{\thepage}

%% now redefine the plain style pages (chapter pages, contents pages)
    \fancypagestyle{plain}{
        \fancyhf{}
        \fancyfoot[RO]{}
        \renewcommand{\headrulewidth}{0pt}
        \renewcommand{\footrulewidth}{0pt}
    }
